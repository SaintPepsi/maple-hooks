/**
 * LoadContext Contract Tests — re-exports from LoadContext.test.ts
 *
 * The contract logic is fully tested in LoadContext.test.ts (42 tests).
 * This file exists so test-enforcement hooks can locate contract tests
 * by the canonical `.contract.test.ts` naming convention.
 */
import "./LoadContext.test";
