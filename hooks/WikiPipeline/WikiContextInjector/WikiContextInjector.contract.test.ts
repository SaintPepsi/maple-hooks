/**
 * WikiContextInjector Contract Tests — re-exports from WikiContextInjector.test.ts
 *
 * The contract logic is fully tested in WikiContextInjector.test.ts (22 tests).
 * This file exists so test-enforcement hooks can locate contract tests
 * by the canonical `.contract.test.ts` naming convention.
 */
import "./WikiContextInjector.test";
